# NexLog Environment Variables
# Скопируй в .env и заполни перед запуском

# JWT secret (сгенерируй: openssl rand -hex 32)
JWT_SECRET=change_me_to_random_64_char_string

# Порт сервера
PORT=3000

# Пути (не менять при использовании Docker)
DATA_DIR=/app/data
PUBLIC_DIR=/app/public
